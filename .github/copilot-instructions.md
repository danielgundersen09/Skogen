# Workspace Instructions

- Use Next.js App Router conventions for this project.
- Keep the landing page responsive across mobile and desktop widths.
- Preserve the forest-inspired palette and the three-link navigation structure when extending the page.
- Run `npm run lint` and `npm run build` after meaningful UI changes.
